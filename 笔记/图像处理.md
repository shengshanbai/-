# 从 bilateral filter 到 HDRnet
双边滤波（Bilateral filter）是一种可以保边去噪的滤波器。Bilateral filter算是比较早的滤镜，但是生命力很强，其内在的思想比较深刻。原始的思路比较简单，也很直观：
$$
\overset{\sim}{I}(p)=\frac{\sum_qI(q)w(p,q)}{\sum_qw(p,q)}
$$
这里$I(p)$代表图像的像素值，$p,q$是图像上的位置坐标，$w(p,q)$表示了和位置相关的权重。一大类降噪算法其实都可以归结到这个公式，关键区别在于权重$w$的选择。